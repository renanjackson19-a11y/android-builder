name: Build GreenPlay 5.28.47 YouTube GreenShorts\n# player-origin-v2

on:
  push:
    paths:
      - '.github/workflows/build-greenplay-52847-youtube-greenshorts.yml'

permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v4
        with:
          cmdline-tools-version: '14742923'
          packages: ''
          accept-android-sdk-licenses: 'true'
          log-accepted-android-sdk-licenses: 'false'

      - name: Download GreenPlay 5.28.45 source
        uses: actions/download-artifact@v4
        with:
          name: GreenPlay-5.28.45-greenshorts-filter
          path: base_artifact
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 35764998704

      - name: Apply 5.28.47 YouTube GreenShorts patch
        shell: bash
        run: |
          set -euo pipefail
          rm -rf work
          mkdir work
          unzip -q base_artifact/GreenPlay_Android_Nativo_5_28_52845_GREENSHORTS_FILTRO.zip -d work
          python3 inspection/patch52847_youtube.py
          grep -q 'versionCode 52847' work/app/build.gradle
          grep -q 'Api.post("greenshorts"' work/app/src/main/java/fun/greenplay/app/MainActivity.java
          test -f work/app/src/main/java/fun/greenplay/app/YouTubePlayerActivity.java

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
          cache-disabled: true

      - name: Compile APK
        shell: bash
        run: |
          set -euo pipefail
          cd work
          gradle assembleDebug --stacktrace --no-daemon

      - name: Package output
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          cp work/app/build/outputs/apk/debug/app-debug.apk out/GreenPlay_5.28.47-debug.apk
          (cd work && zip -qr ../out/GreenPlay_Android_Nativo_5_28_52847_YOUTUBE_GREENSHORTS.zip . -x '.gradle/*' 'app/build/*' 'build/*')
          unzip -t out/GreenPlay_Android_Nativo_5_28_52847_YOUTUBE_GREENSHORTS.zip >/dev/null
          ls -lh out

      - uses: actions/upload-artifact@v4
        with:
          name: GreenPlay-5.28.47-youtube-greenshorts
          path: out/*
          if-no-files-found: error
          retention-days: 30
