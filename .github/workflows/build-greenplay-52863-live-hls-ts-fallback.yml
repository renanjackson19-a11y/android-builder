name: Build GreenPlay 5.28.63 Live HLS-TS Fallback

on:
  push:
    paths:
      - '.github/workflows/build-greenplay-52863-live-hls-ts-fallback.yml'

permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v4
        with:
          cmdline-tools-version: '14742923'
          packages: ''
          accept-android-sdk-licenses: 'true'
          log-accepted-android-sdk-licenses: 'false'
      - name: Download 5.28.62 source
        uses: actions/download-artifact@v4
        with:
          name: GreenPlay-5.28.62-cast-hotfix
          path: base_artifact
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 35803127224
      - name: Patch source
        shell: bash
        run: |
          set -euo pipefail
          rm -rf work
          mkdir work
          unzip -q base_artifact/GreenPlay_Android_Nativo_5_28_52862_CAST_HOTFIX.zip -d work
          python3 inspection/patch52863_live_hls_ts_fallback.py
          grep -q 'versionCode 52863' work/app/build.gradle
          grep -q 'video/mp2t' work/app/src/main/java/fun/greenplay/app/MainActivity.java
          grep -q 'retryLiveAsTs' work/app/src/main/java/fun/greenplay/app/PlayerActivity.java
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
          cache-disabled: true
      - name: Compile APK
        run: cd work && gradle assembleDebug --stacktrace --no-daemon
      - name: Package output
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          cp work/app/build/outputs/apk/debug/app-debug.apk out/GreenPlay_5.28.63-debug.apk
          (cd work && zip -qr ../out/GreenPlay_Android_Nativo_5_28_52863_HLS_TS_FALLBACK.zip . -x '.gradle/*' 'app/build/*' 'build/*')
          unzip -t out/GreenPlay_Android_Nativo_5_28_52863_HLS_TS_FALLBACK.zip >/dev/null
      - uses: actions/upload-artifact@v4
        with:
          name: GreenPlay-5.28.63-live-hls-ts-fallback
          path: out/*
          if-no-files-found: error
          retention-days: 30
